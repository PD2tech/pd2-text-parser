no stats

dmg-min = damage_min to mindamage
dmg-max = damage_max to maxdamage
dmg% = item_damage_percent needs its own string

multiple stats

res-all = all_resist with value for each res but string for all resistances
res-all-max
dmg-fire
dmg-ltng
dmg-mag
dmg-cold
dmg-pois
dmg-norm